import { sendMail } from "@/lib/mail";
import Image from "next/image";

export default async function Home() {
  return (
    <div className="flex min-h-screen flex-col items-center justify-between p-24 bg-gradient-to-r from-green-100 to-purple-100 text-gray-600 h-screen"></div>
  );
}
